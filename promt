Add a new **Customer Listing** page and link it in the **side nav bar**. It should be **identical in layout, behavior, and UI** to the other listing pages (table, search, add/edit/delete, swipe-to-delete, history/save, etc.).

**Only difference:** the **Add Customer** popup must include an **Area** field as a **dropdown select**. The dropdown options must be loaded from the **Area Listing** page’s saved areas.

Requirements:
- The customer list and its add/edit/delete flow must match existing listing pages.
- Area field in the Add/Edit popup:
	- Use a dropdown select.
	- Populate options from saved Area listing data.
	- Store the selected area with the customer record.
- Keep naming/structure consistent with existing listing pages.
- Update side nav to include “Customer Listing”.
